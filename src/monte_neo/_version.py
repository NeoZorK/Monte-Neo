# Single source of truth for the project version.
# Pattern: v0.0.1 -> v0.0.2 -> ... -> v0.0.8 (leading "v" required).
# Git tags and GitHub Releases must match this string exactly.
__version__ = "v0.0.8"
