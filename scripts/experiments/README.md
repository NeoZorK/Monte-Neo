# Experiments

One-off Metal / MLX experiments and micro-benchmarks, kept for reference. They are not part
of the test suite and are not maintained. They need macOS with Apple Silicon and the `apple` extra.

```bash
uv run python scripts/experiments/test_metal_pipeline.py
```
